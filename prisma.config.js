// Prisma configuration file
// This file prevents Prisma from trying to load a non-existent configuration file in the Vercel environment.

export default {
  // Prisma configuration options can be added here if needed
  // For now, we're using an empty configuration to prevent errors
}

// Export as CommonJS module as well for compatibility
module.exports = {
  default: {
    // Prisma configuration options can be added here if needed
    // For now, we're using an empty configuration to prevent errors
  }
}